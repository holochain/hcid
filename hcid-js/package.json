{
  "name": "@holochain/hcid-js",
  "version": "0.0.5",
  "description": "javascript (wasm) build for rust Holochain base32 encoding scheme for keys, agents, identifiers, etc",
  "main": "lib/index.js",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/holochain/hcid.git"
  },
  "dependencies": {},
  "scripts": {
    "test": "standard lib/*.js && mocha lib/*.test.js"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "mocha": "^5.2.0",
    "standard": "^12.0.1"
  },
  "standard": {
    "env": {
      "mocha": true
    },
    "ignore": [
      "lib/bindgen*"
    ]
  }
}
